# jwt.secret has no default here on purpose (never commit a real secret).
# To run locally: create backend/config/application-dev.properties (gitignored)
# with jwt.secret=<your-value>, and spring.datasource.password=<yours> if your
# local MySQL root user isn't passwordless. Spring Boot merges it automatically.
spring.datasource.url=jdbc:mysql://localhost:3306/cars_management
spring.datasource.username=root
spring.datasource.password=
